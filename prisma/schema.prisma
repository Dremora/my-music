generator client {
  provider        = "prisma-client"
  output          = "../src/generated/prisma"
  previewFeatures = ["strictUndefinedChecks", "typedSql"]
}

datasource db {
  provider = "postgresql"
}
